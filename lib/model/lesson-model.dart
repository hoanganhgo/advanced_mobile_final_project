class LessonModel {
  String id;
  String name;
  String content;
  String videoName;
  String videoUrl;
  double hours;

  LessonModel(this.id, this.name, this.content, this.videoName, this.videoUrl,
      this.hours);
}